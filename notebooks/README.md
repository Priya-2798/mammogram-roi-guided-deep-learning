# Notebooks

The numbered folders contain the canonical executed experiment sequence used in the dissertation. Outputs are intentionally preserved because they document the completed runs and reported analyses.

The `legacy/` folder contains earlier/alternate notebooks retained for provenance. It should not be treated as part of the canonical 01–13 execution sequence.
