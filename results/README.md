# Results directory

This repository does not bundle raw datasets, large checkpoints or the full generated results tree. The executed notebooks retain the important displayed outputs used in the dissertation.

When rerunning experiments, write generated CSVs/figures/checkpoints to a local results area and avoid committing large or sensitive artefacts unless specifically required for assessment.
